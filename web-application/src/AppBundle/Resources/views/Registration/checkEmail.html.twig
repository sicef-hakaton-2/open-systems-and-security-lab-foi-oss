{% extends 'AppBundle:Layout:frontAppLayout.html.twig' %}

{% block body %}

    <div id="loginPage1">
        <div class="row-centered fullPageForm">
            <div class="col-md-4 col-centered well">
                <h1>Success!</h1>
                <p class="text-center">{{ 'registration.check_email'|trans({'%email%': user.email}, 'FOSUserBundle') }}</p>
            </div>
        </div>
    </div>

{% endblock body %}
